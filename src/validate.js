function validate(timestamp) {
    var result = {
                    unix: 'unix',
                    natural: 'natural'
                  };
    //validate timestamp here
    return result
}

module.exports = validate;
